
# The location of the mesh library
meshdir := $(LIBMESH_DIR)

# include the library options determined by configure.  This will
# set the variables INCLUDE and LIBS that we will need to build and
# link with the library.
include $(meshdir)/Make.common

libmesh_INCLUDE := -I$(STROMA_DIR)/include -I$(STROMA_DIR)/include/materials $(libmesh_INCLUDE)
libmesh_LIBS := -L$(STROMA_DIR) -lstroma-$(METHOD) $(libmesh_LIBS)

stroma_LIB := $(STROMA_DIR)/libstroma-$(METHOD).a
