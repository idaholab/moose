# AddCorrectorAction

Adds `Corrector` objects into the system that are listed under `Correctors/` in the input file.
This action adds them to the [application](MooseApp.md).

The preferred syntax for adding `Correctors` is to add them in the `[Correctors]` block.

!syntax parameters /Correctors/AddCorrectorAction
