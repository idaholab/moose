#!/bin/bash
#PBS -N <JOB_NAME>
#PBS -l select=<MPI_PROCS>
#PBS -l place=<PLACE>
#PBS -l walltime=<WALLTIME>
<PREREQ>
<PBS_PROJECT>
<COMBINE_STREAMS>

JOB_NUM=${PBS_JOBID%\.*}

export MV2_ENABLE_AFFINITY=0

cd <WORKING_DIR>
<COMMAND>
