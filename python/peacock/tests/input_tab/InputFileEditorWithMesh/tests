[Tests]
  issues = '#7451'
  design = 'python/peacock.md'
  [InputFileEditorWithMesh]
    type = PythonUnitTest
    input = test_InputFileEditorWithMesh.py
    separate = True
    display_required = true
    requirement = "The system shall include a graphical interface that lets users modify input files with an existing mesh, pulling in block information from the mesh and highlighting blocks and boundaries in the mesh when specifying them as parameters."
  []
[]
