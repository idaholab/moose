[pytest]
addopts =
    -vvv
    --cov=moosecontrol
    --cov-config=.coveragerc
testpaths = tests
log_cli = true
log_cli_level = DEBUG
markers =
    moose: marks tests as requiring moose (use --no-moose to skip)
