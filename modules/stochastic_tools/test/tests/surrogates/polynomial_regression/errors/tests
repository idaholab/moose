[Tests]
  design = PolynomialRegressionTrainer.md
  issues = '#15361'

  [errors]
    requirement = "The system shall throw an error when the number of samples does not match the number of results."

    [wrong_sampler]
      type = RunException
      input = error.i
      allow_test_objects = true
      expect_err = "Sampler number of rows does not match number of results from vector postprocessor."
    []
  []
[]
