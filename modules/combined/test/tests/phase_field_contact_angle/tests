[Tests]
  issues = "#31300"
  design = "ADPhaseFieldContactAngleBC.md ObtainAvgContactAngle.md ADPhaseFieldTwoPhaseSurfaceTension.md ADPhaseFieldTwoPhaseMaterial.md"
  [contact_angle]
    type = CSVDiff
    input = contact_angle.i
    csvdiff = contact_angle_csv.csv
    # The refined interface mesh and its direct factorization need more memory than one
    # slot is allowed, and the TestHarness measures that allowance per slot.
    min_parallel = 8
    capabilities = 'mumps'
    restep = false # Test is prescribed to abort on failed timestep
    max_time = 600
    heavy = true
    requirement = "The ADPhaseFieldContactAngleBC kernel correctly implements the prescribed contact angle."
  []
[]
