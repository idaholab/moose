[Tests]
  design = 'QuadSubChannel1PhaseProblem.md'
  issues = '#29196 #29199 #29237 #29505'
  [psbt_regression_test_explicit]
    type = CSVDiff
    input = psbt_explicit.i
    csvdiff = psbt_explicit_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the explicit subchannel solver for a psbt type case'
  []
  [psbt_regression_test_explicit_friction2]
    type = CSVDiff
    input = psbt_explicit_v2.i
    csvdiff = psbt_explicit_v2_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the explicit subchannel solvers for a psbt type case using the non-default friction/mixing model'
  []
  [psbt_regression_test_explicit_staggered]
    type = CSVDiff
    input = psbt_explicit_staggered.i
    csvdiff = psbt_explicit_staggered_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the explicit subchannel solver in a staggered pressure formulation for a psbt type case'
  []
  [psbt_regression_test_implicit]
    type = CSVDiff
    input = psbt_implicit.i
    csvdiff = psbt_implicit_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the implicit subchannel solver for a psbt type case'
  []
  [psbt_regression_test_monolithic]
    type = CSVDiff
    input = psbt_monolithic.i
    csvdiff = psbt_monolithic_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the monolithic subchannel solver for a psbt type case'
  []
  [psbt_regression_test_full_monolithic]
    type = CSVDiff
    input = psbt_full_monolithic.i
    csvdiff = psbt_full_monolithic_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the monolithic subchannel solver for a psbt type case'
  []
  [psbt_regression_test_full_monolithic_staggered]
    type = CSVDiff
    input = psbt_full_monolithic_staggered.i
    csvdiff = psbt_full_monolithic_staggered_out.csv
    capabilities = 'method!=dbg'
    valgrind = NONE
    recover = false
    abs_zero = 1e-6
    max_threads = 1
    max_parallel = 1
    requirement = 'The system will examine the monolithic subchannel solver for a psbt type case'
  []
[]
