[Tests]
  design = 'SIMPLE.md PIMPLE.md'
  issues = '#32151'
  [all_equations]
    type = CSVDiff
    input = pipe_flow_energy_scalars.i
    csvdiff = 'all_equations_line_sampler_0002.csv'
    cli_args = 'Outputs/file_base=all_equations'
    requirement = 'The system shall be able to converge a steady segregated flow, energy, and passive scalar solution suitable for restart.'
    max_threads = 1
    recover = false # Using a SIMPLE solver
  []

  [scalar_off]
    type = CSVDiff
    input = pipe_flow_energy_scalars.i
    prereq = all_equations
    csvdiff = 'scalar_off_line_sampler_0002.csv'
    cli_args = 'Outputs/file_base=scalar_off Executioner/should_solve_passive_scalars=false'
    requirement = 'The system shall be able to disable passive scalar solves, keeping the scalar field equal to its initial condition.'
    max_threads = 1
    recover = false # Using a SIMPLE solver
  []

  [restart_scalar_only]
    type = CSVDiff
    input = pipe_flow_energy_scalars.i
    prereq = scalar_off
    csvdiff = 'restart_scalar_only_line_sampler_0002.csv'
    cli_args = "Problem/restart_file_base=scalar_off_cp/LATEST
                Outputs/file_base=restart_scalar_only"
    requirement = 'The system shall be able to restart only the passive scalar solution and reproduce the same converged state as a fully coupled run.'
    max_threads = 1
    recover = false # Using a SIMPLE solver
  []
[]
