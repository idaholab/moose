[Tests]
  design = 'MeshMetaDataInterface.md'
  issues = '#23447'

  [has_property]
    type = RunApp
    input = 'mmdi.i'
    cli_args = 'UserObjects/test/has_property="foo addmd"'

    requirement = 'The system shall provide an interface to check whether or no a mesh property exists'
  []

  [errors]
    requirement = 'The system shall report a reasonable error when retreiving a property of the mesh when'

    [get_wrong_type]
      type = RunException
      input = 'mmdi.i'
      cli_args = 'UserObjects/test/get_wrong_type="foo addmd"'
      expect_err = "the property exists with different type 'double'"
      detail = 'the underlying types differ'
    []
  []
[]
