from options import *

test = { INPUT : 'block_kernel_test.i',
         EXODIFF : ['out.e'],
         SCALE_REFINE : 4
         }

testvars = { INPUT : 'block_vars.i',
             EXODIFF : ['out_vars.e'],
             SCALE_REFINE : 4
             }

