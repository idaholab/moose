[Tests]
  issues = '#12293'
  design = 'Transfers/index.md'
  [both-transformed]
    type = RunException
    input = main-app.i
    requirement = 'The system shall output a warning if a transfer that only matches node ids is used in combinated with a coordinate transformation.'
    expect_err = "'to_sub' of type 'MultiAppCopyTransfer' has non-translation transformations but it does not leverage the 'MooseCoordTransform' class. Your data transfers may be inaccurate"
    mesh_mode = replicated
  []
[]
