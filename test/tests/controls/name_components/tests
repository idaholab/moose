[Tests]
   [./system_object_param]
    # Test that all aspects of the name may be specified using base class name
    type = Exodiff
    input = system_object_param.i
    exodiff = system_object_param_out.e
  [../]

  [./syntax_object_param]
    # Test that all aspects of the name may be specified using input syntax
    type = Exodiff
    input = syntax_object_param.i
    exodiff = syntax_object_param_out.e
  [../]

  [./object_param]
    # Test that all objects with the same name change
    type = Exodiff
    input = object_param.i
    exodiff = object_param_out.e
  [../]

  [./param]
    # Test that all parameters with the same name change
    type = Exodiff
    input = param.i
    exodiff = param_out.e
  [../]

  [./system_param]
    # Test that all params in a system with the same name change using base class name
    type = Exodiff
    input = system_param.i
    exodiff = system_param_out.e
  [../]

  [./syntax_param]
    # Test that all params in a system with the same name change using input file syntax
    type = Exodiff
    input = syntax_param.i
    exodiff = syntax_param_out.e
  [../]
[]
