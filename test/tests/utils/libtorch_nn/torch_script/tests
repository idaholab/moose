[Tests]
  design = 'LibtorchTorchScriptNeuralNet.md'
  issues = '#19571'

  [libtorch-torch-script]
    type = CSVDiff
    input = 'test.i'
    csvdiff = 'test_out_test_0001.csv'
    requirement = 'The system shall be able to compute the same values with a neural net as the python code which created the neural net.'
    libtorch = true
    allow_test_objects = True
  []
[]
